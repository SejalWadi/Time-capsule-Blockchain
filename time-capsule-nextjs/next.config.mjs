/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
      allowedDevOrigins: ['http://192.168.1.8:3000'],  // Replace with your local IP address
    },
  };
  
  export default nextConfig;
  